# Changelog

## 0.0.32

### 新增

- 新增「提及」面板模式：与原有「引用」（反链）模式并列，可查询提及了当前文档但未直接引用的内容，独立筛选与分页。
- 文档树右键菜单新增「反链过滤面板」二级菜单，可对选中的一篇或多篇文档批量设置底部反链面板的显示/隐藏/恢复默认，避免与其他插件的选项混在一起。

### 修复

- 修复关联定义块在嵌套列表项场景下的筛选范围 bug：当祖先路径（父级列表项等）已经满足包含/排除条件时，如果当前列表项节点自身内容为空，会被误判为不匹配，导致点击关联定义块后反链/提及数据错误地显示为空。
- 修复关联定义块的计数范围未包含「所在文档自身」（标题、命名、别名、标签）中的引用，导致这部分关联定义块只能显示数量、点击筛选后却无对应数据的问题。现已将文档自身范围纳入计数与筛选，与父路径范围保持一致。
